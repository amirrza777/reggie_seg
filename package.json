{
  "devDependencies": {
    "husky": "^9.1.7",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "check:duplicates": "node scripts/check-duplicate-files.mjs",
    "check:lockfiles": "node scripts/check-lockfile-policy.mjs",
    "prepare": "husky",
    "scan:functions": "bash scripts/scan-functions.sh"
  }
}
