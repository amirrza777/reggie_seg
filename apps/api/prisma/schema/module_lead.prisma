model ModuleLead {
  moduleId Int
  userId   Int
  module   Module @relation(fields: [moduleId], references: [id])
  user     User   @relation(fields: [userId], references: [id])

  @@id([moduleId, userId])
  @@index([userId], map: "ModuleLead_userId_fkey")
}
