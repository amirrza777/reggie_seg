model UserModule {
  userId   Int
  moduleId Int
  user     User   @relation(fields: [userId], references: [id]) @ignore
  module   Module @relation(fields: [moduleId], references: [id]) @ignore

  @@id([userId, moduleId])
  @@index([moduleId])
}
